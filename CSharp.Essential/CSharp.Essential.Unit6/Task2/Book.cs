using System;

namespace CSharp.Essential.Unit6
{
    partial class Book
    {
        public void FindNext(string str)
        {
            Console.WriteLine("Поиск строки : " + str);
        }
    }
}